<% content_for :title, "Accountable To - Stay Committed to Your Goals" %>

<div class="hero-section">
  <div class="container">
    <div class="hero-content">
      <h1 class="hero-title">
        Stay <span class="highlight">Accountable</span> to Your Goals
      </h1>
      <p class="hero-subtitle">
        Transform your aspirations into achievements with personalized check-ins and AI-powered guidance
      </p>

      <div class="features-grid">
        <div class="feature">
          <div class="feature-icon">🎯</div>
          <h3>Set Clear Goals</h3>
          <p>Define what matters most to you with specific, actionable objectives</p>
        </div>
        <div class="feature">
          <div class="feature-icon">📅</div>
          <h3>Regular Check-ins</h3>
          <p>Choose how often we check in - daily, weekly, or monthly</p>
        </div>
        <div class="feature">
          <div class="feature-icon">🤖</div>
          <h3>AI-Powered Support</h3>
          <p>Get personalized insights and motivation to keep you on track</p>
        </div>
      </div>
    </div>

    <div class="signup-form">
      <h2>Start Your Journey Today</h2>
      <p>Join thousands who are already achieving their goals</p>

      <%= form_with model: @user, url: signup_path, local: true, class: "email-form" do |form| %>
        <div class="form-group">
          <%= form.email_field :email,
              placeholder: "Enter your email address",
              class: "email-input #{'error' if @user.errors[:email].any?}",
              required: true %>
          <%= form.submit "Get Started Free", class: "submit-btn" %>
        </div>
      <% end %>

      <% if @existing_user %>
        <div class="existing-user-message">
          <p>This email is already registered!</p>
          <% if @existing_user.confirmed? %>
            <%= form_with url: send_magic_link_path, method: :post, local: true, class: "magic-link-form" do |form| %>
              <%= form.hidden_field :email, value: @existing_user.email %>
              <%= form.submit "Send Magic Link to Login", class: "magic-link-btn" %>
            <% end %>
            <p class="magic-link-note">We'll send you a secure link to login instantly</p>
          <% else %>
            <p class="confirmation-note">Please check your email to confirm your account first.</p>
          <% end %>
        </div>
      <% elsif @user.errors[:email].any? %>
        <div class="error-message">
          <%= @user.errors[:email].first %>
        </div>
      <% end %>

      <p class="privacy-note">
        ✓ Free forever &nbsp;&nbsp; ✓ No spam &nbsp;&nbsp; ✓ Unsubscribe anytime
      </p>
    </div>
  </div>
</div>

<div class="how-it-works">
  <div class="container">
    <h2>How It Works</h2>
    <div class="steps">
      <div class="step">
        <div class="step-number">1</div>
        <h3>Sign Up</h3>
        <p>Enter your email and confirm your account</p>
      </div>
      <div class="step">
        <div class="step-number">2</div>
        <h3>Set Your Goal</h3>
        <p>Tell us what you want to achieve and how often to check in</p>
      </div>
      <div class="step">
        <div class="step-number">3</div>
        <h3>Stay Accountable</h3>
        <p>Receive regular check-ins and AI-powered guidance</p>
      </div>
    </div>
  </div>
</div>

<style>
  .hero-section {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    padding: 80px 0;
    min-height: 100vh;
    display: flex;
    align-items: center;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 60px;
    align-items: center;
  }

  .hero-title {
    font-size: 3.5rem;
    font-weight: 700;
    margin-bottom: 20px;
    line-height: 1.2;
  }

  .highlight {
    color: #fbbf24;
  }

  .hero-subtitle {
    font-size: 1.25rem;
    margin-bottom: 40px;
    opacity: 0.9;
    line-height: 1.6;
  }

  .features-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 20px;
  }

  .feature {
    display: flex;
    align-items: flex-start;
    gap: 15px;
  }

  .feature-icon {
    font-size: 2rem;
    min-width: 50px;
  }

  .feature h3 {
    margin: 0 0 8px 0;
    font-size: 1.1rem;
  }

  .feature p {
    margin: 0;
    opacity: 0.8;
    font-size: 0.95rem;
  }

  .signup-form {
    background: white;
    color: #374151;
    padding: 40px;
    border-radius: 16px;
    box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
  }

  .signup-form h2 {
    margin: 0 0 10px 0;
    font-size: 1.75rem;
    font-weight: 600;
  }

  .signup-form > p {
    margin: 0 0 30px 0;
    color: #6b7280;
  }

  .form-group {
    display: flex;
    gap: 12px;
    margin-bottom: 20px;
  }

  .email-input {
    flex: 1;
    padding: 16px;
    border: 2px solid #e5e7eb;
    border-radius: 8px;
    font-size: 1rem;
    transition: border-color 0.2s;
  }

  .email-input:focus {
    outline: none;
    border-color: #4f46e5;
    box-shadow: 0 0 0 3px rgba(79, 70, 229, 0.1);
  }

  .email-input.error {
    border-color: #ef4444;
  }

  .submit-btn {
    background: #4f46e5;
    color: white;
    border: none;
    padding: 16px 24px;
    border-radius: 8px;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    transition: background-color 0.2s;
    white-space: nowrap;
  }

  .submit-btn:hover {
    background: #4338ca;
  }

  .error-message {
    color: #ef4444;
    font-size: 0.875rem;
    margin-top: -10px;
    margin-bottom: 10px;
  }

  .existing-user-message {
    background: #fef3c7;
    border: 1px solid #f59e0b;
    border-radius: 8px;
    padding: 20px;
    margin-top: -10px;
    margin-bottom: 10px;
  }

  .existing-user-message p {
    margin: 0 0 15px 0;
    color: #92400e;
    font-weight: 500;
  }

  .magic-link-form {
    margin-bottom: 10px;
  }

  .magic-link-btn {
    background: #059669;
    color: white;
    border: none;
    padding: 12px 20px;
    border-radius: 6px;
    font-size: 0.875rem;
    font-weight: 500;
    cursor: pointer;
    transition: background-color 0.2s;
    width: 100%;
  }

  .magic-link-btn:hover {
    background: #047857;
  }

  .magic-link-note {
    color: #6b7280;
    font-size: 0.75rem;
    margin: 8px 0 0 0;
    text-align: center;
  }

  .confirmation-note {
    color: #6b7280;
    font-size: 0.875rem;
    margin: 0;
    text-align: center;
  }

  .privacy-note {
    color: #6b7280;
    font-size: 0.875rem;
    text-align: center;
    margin: 20px 0 0 0;
  }

  .how-it-works {
    padding: 80px 0;
    background: #f9fafb;
  }

  .how-it-works .container {
    display: block;
    text-align: center;
  }

  .how-it-works h2 {
    font-size: 2.5rem;
    margin-bottom: 50px;
    color: #1f2937;
  }

  .steps {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 40px;
    max-width: 800px;
    margin: 0 auto;
  }

  .step {
    text-align: center;
  }

  .step-number {
    width: 60px;
    height: 60px;
    background: #4f46e5;
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    font-weight: 600;
    margin: 0 auto 20px;
  }

  .step h3 {
    font-size: 1.25rem;
    margin-bottom: 10px;
    color: #1f2937;
  }

  .step p {
    color: #6b7280;
    line-height: 1.6;
  }

  @media (max-width: 768px) {
    .container {
      grid-template-columns: 1fr;
      gap: 40px;
    }

    .hero-title {
      font-size: 2.5rem;
    }

    .form-group {
      flex-direction: column;
    }

    .steps {
      grid-template-columns: 1fr;
      gap: 30px;
    }
  }
</style>
